name: 📡 Sinais da Matrix

on:
  schedule:
    - cron: "0 6 * * *"   # Executa diariamente às 06:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  issues: read
  pull-requests: read
  actions: read

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Saída
          filename: assets/metrics.svg
          token: ${{ secrets.GITHUB_TOKEN }}

          # Base
          base: activity, repositories

          # ─────────────────────────────────────────
          # Coding habits + Recent activity
          # ─────────────────────────────────────────
          plugin_habits: yes
          plugin_habits_from: 90
          plugin_habits_days: 10
          plugin_habits_facts: yes
          plugin_habits_charts: yes

          # ─────────────────────────────────────────
          # Isometric Commit Calendar (ano completo)
          # ─────────────────────────────────────────
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          # ─────────────────────────────────────────
          # Languages Activity (atividade recente)
          # ─────────────────────────────────────────
          plugin_languages: yes
          plugin_languages_limit: 5
          plugin_languages_details: bytes-size, percentage
          plugin_languages_recent_days: 30
          plugin_languages_recent_load: 300
          plugin_languages_ignored: html, css

          # ─────────────────────────────────────────
          # Google PageSpeed (DESATIVADO POR ENQUANTO)
          # ─────────────────────────────────────────
          # plugin_pagespeed: yes
          # plugin_pagespeed_url: https://plus.spsp.com.br
          # plugin_pagespeed_screenshot: yes

          # ─────────────────────────────────────────
          # AniList (DESATIVADO POR ENQUANTO)
          # ─────────────────────────────────────────
          # plugin_anilist: yes
          # plugin_anilist_user: SEU_USUARIO_ANILIST
          # plugin_anilist_sections: watching, reading
          # plugin_anilist_limit: 5
